import React from 'react';

/**
 * Clase App que pinta la pantalla principal
 */
class App extends React.Component {

    /**
     * Constructor
     * @param {Propiedades} props 
     */
    constructor(props) {
        super(props);
    }

    /**
     * Método render que renderiza la vista 
     */
   render() { 
      return (
          <div className="App">
              
          </div>
      );
    }
};

export default App;
